//! The clap argument struct, `GlobalFlags` flattening, and the
//! unsupported-flag refusals docs/curl.md's "Literate errors" section
//! catalogs.
//!
//! Empty stub. Parsing curl's flag surface — and refusing the flags this
//! build does not carry with the exact literate messages docs/curl.md
//! specifies — is HTTP-surface work and waits on the review docs/curl.md's
//! "Status" section calls for. Nothing is declared here yet rather than
//! guessing a clap struct shape the review might reshape.
